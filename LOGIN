<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(180deg, #000000, #3d3d3d);
            color: white;
        }

        .container {
            text-align: center;
            width: 300px;
            padding: 20px;
            background: #121212;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.6);
        }

        .container img {
            width: 120px;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 24px;
            margin: 10px 0;
        }

        p {
            margin: 5px 0 20px;
            color: #cccccc;
        }

        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            outline: none;
            background: #2d2d2d;
            color: white;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border: none;
            border-radius: 5px;
            background: #0078ff;
            color: white;
            font-size: 16px;
            cursor: pointer;
        }

        .social-login {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 15px;
        }

        .social-login img {
            width: 30px;
            cursor: pointer;
        }

        .register {
            margin-top: 15px;
            color: #0078ff;
            cursor: pointer;
        }

        .register:hover {
            text-decoration: underline;
        }

    </style>
</head>
<body>
    <div class="container">
        <img src="robot.png" alt="Robot" /> <!-- Replace with your robot image file name -->
        <h1>LOGIN</h1>
        <p>Please Login to Continue</p>
        <form>
            <input type="text" placeholder="Username" required />
            <input type="password" placeholder="Password" required />
            <button type="submit">Login</button>
        </form>

        <p>Or Login With</p>
        <div class="social-login">
            <img src="google-icon.png" alt="Google" /> <!-- Replace with your Google icon -->
            <img src="facebook-icon.png" alt="Facebook" /> <!-- Replace with your Facebook icon -->
        </div>

        <div class="register">
            <p>Don't have an account? <span>Register</span></p>
        </div>
    </div>
</body>
</html>
